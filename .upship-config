# UP SHIP! Playtest Configuration
# This file configures the playtest tool behavior
#
# By default, playtest uses the local server (localhost:3000).
# To use production instead, uncomment the line below:
# UPSHIP_PROD=1
